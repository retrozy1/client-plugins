{
    "name": "client-plugins",
    "devDependencies": {
        "@gimloader/build": "^1.1.1",
        "@types/gimloader": "^1.8.2"
    },
    "description": "Official client-side plugins for Gimloader",
    "scripts": {
        "typecheck": "tsc",
        "buildAll": "bun run --workspaces build"
    },
    "workspaces": [
        "libraries/*",
        "plugins/*"
    ]
}